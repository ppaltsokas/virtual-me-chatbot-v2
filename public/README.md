# Public Assets

Place your photo here as `panos-photo.jpg` (or `.png`).

The chat interface will automatically use this image. If the image is not found, it will fall back to an icon.

Supported formats: `.jpg`, `.jpeg`, `.png`

