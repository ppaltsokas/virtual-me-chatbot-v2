# Force Railpack to use Node.js
[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["cd services/frontend && npm ci"]

[phases.build]
cmds = ["cd services/frontend && npm run build"]

[phases.start]
cmds = ["cd services/frontend && sh start-frontend.sh"]
